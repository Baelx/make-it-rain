
       {% for key,product in order.products %}
         {% if product.name not in ['Payment costs', 'Coûts de paiement'] %} {# Make sure to comment your changes #}
            
            {% Contents of product loop removed #}
            
          {% endif %} {# End comment #}
        {% endfor %}
